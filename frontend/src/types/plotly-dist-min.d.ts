// plotly.js-dist-min ships a prebuilt bundle without type definitions. We only
// pass it to react-plotly.js's factory, so an untyped module declaration is
// sufficient here.
declare module 'plotly.js-dist-min';
